name: testing branch synchronization 
on:
  push:
    branches: [main]
  pull_request:
    branches: [test1 test2]  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: test sync
      run: |
          cat testfile
