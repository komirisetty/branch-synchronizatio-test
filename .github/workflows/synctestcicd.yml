name: testing branch synchronization 
on:
  push:
    branches: [main]
  pull_request:
    branches: [test test2]  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: test sync
      run: |
          echo "hello babu"
